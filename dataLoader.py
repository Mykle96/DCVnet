import torch
import numpy
import math
import tqdm


class DataLoader(torch.utils.data.DataLoader):
    def __init__(self):
        # data loading
        return

    def __len__(self):
        # length of dataset
        return
